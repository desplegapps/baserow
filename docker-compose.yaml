version: '3.4'
services:
  baserow:
    container_name: baserow
    image: 'baserow/baserow:1.26.1'
    environment:
      - 'BASEROW_PUBLIC_URL=${URL_SERVICE}'
    volumes:
      - 'baserow_data:/baserow/data'
volumes:
  baserow_data: null
